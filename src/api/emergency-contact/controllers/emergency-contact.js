'use strict';

/**
 * emergency-contact controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::emergency-contact.emergency-contact');
